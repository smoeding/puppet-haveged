# This file is managed by Puppet.

[Service]

# Clear startup command provided by system config
ExecStart=

# Define the new startup command
ExecStart=/usr/sbin/haveged --Foreground --verbose=1 <%= $opts %>
